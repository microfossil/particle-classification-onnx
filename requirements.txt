# Core dependencies with exact versions for reproducibility
# Using onnxruntime-gpu which falls back to CPU if CUDA is not available
numpy==2.3.5
Pillow==12.0.0
onnxruntime==1.23.2
tqdm==4.67.1
pydantic==2.12.5
click==8.3.1

# Note: onnxruntime-gpu will automatically fall back to CPU if CUDA is not available
